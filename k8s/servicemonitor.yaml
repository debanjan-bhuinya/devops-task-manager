apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-manager-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: devops-task-manager
  endpoints:
  - port: http
    path: /metrics
    interval: 10s
